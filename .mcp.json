{
  "mcpServers": {
    "mvp": {
      "command": "uv",
      "args": ["run", "--directory", "mcp-server", "python", "server.py"],
      "env": {
        "MVP_BACKEND_URL": "http://localhost:8000"
      }
    }
  }
}
