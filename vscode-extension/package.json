{
  "name": "maximum-virtual-blueprint",
  "displayName": "Maximum Virtual Blueprint",
  "description": "AI-powered research and product blueprint system inside VS Code",
  "version": "0.2.0",
  "publisher": "Pineapple-Labs",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/arnabgho/maximum-virtual-product"
  },
  "icon": "resources/icon.png",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Other", "Data Science", "Machine Learning"],
  "keywords": ["research", "ai", "planning", "blueprint"],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "mvp-sidebar",
          "title": "MVB",
          "icon": "resources/icon.svg"
        }
      ]
    },
    "views": {
      "mvp-sidebar": [
        {
          "id": "mvp-projects",
          "name": "Projects",
          "type": "tree"
        }
      ]
    },
    "commands": [
      { "command": "mvp.signIn", "title": "MVB: Sign In with GitHub", "icon": "$(sign-in)" },
      { "command": "mvp.signOut", "title": "MVB: Sign Out", "icon": "$(sign-out)" },
      { "command": "mvp.createProject", "title": "MVB: Create Project", "icon": "$(add)" },
      { "command": "mvp.startResearch", "title": "MVB: Start Research" },
      { "command": "mvp.generatePlan", "title": "MVB: Generate Plan" },
      { "command": "mvp.openCanvas", "title": "MVB: Open Canvas" },
      { "command": "mvp.exportPlan", "title": "MVB: Export Plan to Workspace" },
      { "command": "mvp.openInBrowser", "title": "MVB: Open in Browser" },
      { "command": "mvp.refresh", "title": "Refresh", "icon": "$(refresh)" }
    ],
    "menus": {
      "view/title": [
        { "command": "mvp.createProject", "when": "view == mvp-projects", "group": "navigation" },
        { "command": "mvp.refresh", "when": "view == mvp-projects", "group": "navigation" }
      ],
      "view/item/context": [
        { "command": "mvp.startResearch", "when": "viewItem == project-research" },
        { "command": "mvp.generatePlan", "when": "viewItem == project-plan" },
        { "command": "mvp.openCanvas", "when": "viewItem == project" },
        { "command": "mvp.exportPlan", "when": "viewItem == project" },
        { "command": "mvp.openInBrowser", "when": "viewItem == project" }
      ]
    },
    "configuration": {
      "title": "MVB",
      "properties": {
        "mvp.backendUrl": {
          "type": "string",
          "default": "https://selfless-determination-production-af9e.up.railway.app",
          "description": "MVB backend API URL"
        },
        "mvp.frontendUrl": {
          "type": "string",
          "default": "https://www.mvb-global.com",
          "description": "MVB frontend web URL"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile && cd webview && npm run build",
    "compile": "tsc -p ./tsconfig.json",
    "watch": "tsc -watch -p ./tsconfig.json",
    "package": "npx @vscode/vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "@types/ws": "^8.5.0",
    "typescript": "~5.8.0"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
